./serve.sh
./serve.sh --no-open
# For use on the VPS
./process_static.sh install-gz
./process_static.sh "$@"
# Run as *root*
check: sudo nginx -t
reload: sudo nginx -s reload
# Add nginx http authentication password
htpasswd -bB ./nginx/etc/htpasswd "$1" "$2" # Username Password
